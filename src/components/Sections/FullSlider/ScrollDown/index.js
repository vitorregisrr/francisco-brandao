import React from 'react';

import './styles.scss';

const ScrollDown = (props) => {
    return (
        <button className="ScrollDown">
        
        </button>
    )
}

export default ScrollDown;